# Placeholder Dockerfile for web frontend.
# Replace with framework-specific build steps.
FROM node:22-alpine
WORKDIR /app
COPY . .
EXPOSE 3000
CMD ["sh", "-c", "echo Replace Dockerfile steps for frontend/web && sleep infinity"]
