# Placeholder Dockerfile for svc-core.
# Replace with language/runtime-specific build steps.
FROM eclipse-temurin:21-jre
WORKDIR /app
COPY . .
EXPOSE 8080
CMD ["sh", "-c", "echo Replace Dockerfile steps for svc-core && sleep infinity"]
