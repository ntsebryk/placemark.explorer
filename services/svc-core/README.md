# svc-core

Core backend service.

## Responsibilities

- API and domain logic for core placemark features.

## Local run

Use the service-specific runtime command once language/runtime setup is available.

## Environment

Copy `.env.example` to `.env` and configure required values.
